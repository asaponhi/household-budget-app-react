// This file demonstrates available snippets
// Try typing these and pressing Tab:

// rafce + Tab = React Arrow Function Component Export
// rfc + Tab = React Function Component
// useState + Tab = useState hook
// useEffect + Tab = useEffect hook
// imr + Tab = import React from 'react'

// Example of what rafce + Tab creates:

const ComponentName = () => {
  return <div></div>;
};

export default ComponentName;
